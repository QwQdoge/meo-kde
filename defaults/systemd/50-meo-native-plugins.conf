[Service]
# Let the next normal Plasma login discover Meo's per-user native modules.
# This file does not restart KWin, Plasma, or the user session.
Environment=QT_PLUGIN_PATH=%h/.local/lib/qt6/plugins:/usr/lib/qt6/plugins
